diff --git a/public/robots.txt b/public/robots.txt
new file mode 100644
index 0000000..e36390d
--- /dev/null
+++ b/public/robots.txt
@@ -0,0 +1,4 @@
+User-agent: *
+Allow: /
+
+Sitemap: https://nikkei-note.com/sitemap-index.xml
diff --git a/src/layouts/BlogPost.astro b/src/layouts/BlogPost.astro
index 1536db5..fbfc94f 100644
--- a/src/layouts/BlogPost.astro
+++ b/src/layouts/BlogPost.astro
@@ -67,10 +67,21 @@ const relatedPosts = allPosts
   .filter(p => p.data.category === category && p.id !== currentSlug)
   .slice(0, 3);
 
-// 用語解説記事（存在する場合のみ表示）
-const glossaryPosts = allPosts
-  .filter(p => p.data.category === 'glossary')
-  .slice(0, 4);
+// 用語解説記事（タグ関連を優先、なければ最新）
+const currentTags = (Astro.props.tags ?? []) as string[];
+const allGlossary = allPosts.filter(p => p.data.category === 'glossary');
+const tagRelatedGlossary = allGlossary.filter(p =>
+  p.data.tags?.some(t => currentTags.includes(t))
+).slice(0, 4);
+const glossaryPosts = tagRelatedGlossary.length >= 2
+  ? tagRelatedGlossary
+  : allGlossary.slice(0, 4);
+
+// 前後の記事（同カテゴリー）
+const sameCatPosts = allPosts.filter(p => p.data.category === category);
+const currentIdx = sameCatPosts.findIndex(p => p.id === currentSlug);
+const prevPost = currentIdx >= 0 && currentIdx < sameCatPosts.length - 1 ? sameCatPosts[currentIdx + 1] : undefined;
+const nextPost = currentIdx > 0 ? sameCatPosts[currentIdx - 1] : undefined;
 ---
 
 <html lang="ja">
@@ -453,6 +464,43 @@ const glossaryPosts = allPosts
       }
       .glossary-link:hover { background: #d5e8f8; }
 
+      /* 前後記事ナビ */
+      .post-nav {
+        border-top: 1px solid var(--border);
+        margin-top: 28px;
+        padding-top: 20px;
+      }
+      .post-nav-inner {
+        display: grid;
+        grid-template-columns: 1fr 1fr;
+        gap: 12px;
+      }
+      .post-nav-item {
+        display: flex;
+        flex-direction: column;
+        gap: 4px;
+        padding: 14px 16px;
+        border: 1px solid var(--border);
+        border-radius: 12px;
+        background: var(--blue-2);
+        transition: box-shadow .15s;
+      }
+      .post-nav-item:hover { box-shadow: var(--shadow); }
+      .post-nav-next { text-align: right; }
+      .post-nav-label {
+        font-size: 11px;
+        color: var(--muted);
+      }
+      .post-nav-title {
+        font-size: 13px;
+        font-weight: 700;
+        color: var(--navy);
+        line-height: 1.5;
+      }
+      @media (max-width: 860px) {
+        .post-nav-inner { grid-template-columns: 1fr; }
+      }
+
       /* アフィリエイトCTA */
       .aff-cta {
         background: linear-gradient(135deg, #f7fbff, #eaf2fb);
@@ -745,6 +793,26 @@ const glossaryPosts = allPosts
               </div>
             )}
 
+            <!-- 前後記事ナビ -->
+            {(prevPost || nextPost) && (
+              <div class="post-nav">
+                <div class="post-nav-inner">
+                  {prevPost ? (
+                    <a class="post-nav-item post-nav-prev" href={`/blog/${prevPost.id}/`}>
+                      <span class="post-nav-label">← 前の記事</span>
+                      <span class="post-nav-title">{prevPost.data.title}</span>
+                    </a>
+                  ) : <div />}
+                  {nextPost ? (
+                    <a class="post-nav-item post-nav-next" href={`/blog/${nextPost.id}/`}>
+                      <span class="post-nav-label">次の記事 →</span>
+                      <span class="post-nav-title">{nextPost.data.title}</span>
+                    </a>
+                  ) : <div />}
+                </div>
+              </div>
+            )}
+
             <!-- アフィリエイトCTA（nikkei・deep記事のみ） -->
             {(category === 'nikkei' || category === 'deep') && (
               <div class="aff-cta">
diff --git a/src/layouts/GlossaryPost.astro b/src/layouts/GlossaryPost.astro
index 42cc476..2394f16 100644
--- a/src/layouts/GlossaryPost.astro
+++ b/src/layouts/GlossaryPost.astro
@@ -5,7 +5,7 @@ import BaseHead from '../components/BaseHead.astro';
 import SiteHeader from '../components/SiteHeader.astro';
 import '../styles/glossary.css';
 
-type Props = CollectionEntry<'blog'>['data'] & { readTimeMin?: number };
+type Props = CollectionEntry<'blog'>['data'] & { readTimeMin?: number; body?: string };
 
 const {
   title,
@@ -16,6 +16,7 @@ const {
   relatedDeep,
   relatedGlossary,
   readTimeMin,
+  body = '',
 } = Astro.props;
 
 const formattedDate = pubDate
@@ -27,6 +28,9 @@ const currentSlug = currentPath.replace(/^\/blog\//, '').replace(/\/$/, '');
 const ogpImage = currentSlug ? `/og/${currentSlug}.png` : '/blog-placeholder-1.jpg';
 
 const allPosts = await getCollection('blog');
+const sortedGlossary = allPosts
+  .filter((p) => p.data.category === 'glossary')
+  .sort((a, b) => a.data.pubDate.valueOf() - b.data.pubDate.valueOf());
 
 // 関連深掘りノート
 let relatedDeepPost: CollectionEntry<'blog'> | undefined;
@@ -42,7 +46,35 @@ const relatedGlossaryPosts = (relatedGlossary ?? []).map((termName) => {
   return { name: termName, post };
 });
 
+// 前後の用語解説（同カテゴリー内）
+const currentIdx = sortedGlossary.findIndex((p) => p.id === currentSlug);
+const prevGlossary = currentIdx > 0 ? sortedGlossary[currentIdx - 1] : undefined;
+const nextGlossary = currentIdx >= 0 && currentIdx < sortedGlossary.length - 1 ? sortedGlossary[currentIdx + 1] : undefined;
+
+// 最新の日経ノート
+const latestNikkei = allPosts
+  .filter((p) => p.data.category === 'nikkei')
+  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())[0];
+
 const hasRelated = relatedDeepPost || relatedGlossaryPosts.length > 0;
+
+// caution-box から「誤：X 正：Y」ペアを抽出して FAQPage 用に整形
+const faqItems: { question: string; answer: string }[] = [];
+const cautionBlocks = body.match(/<div class="caution-box">([\s\S]*?)<\/div>/g) ?? [];
+for (const block of cautionBlocks) {
+  const text = block.replace(/<[^>]+>/g, '').replace(/⚠\s*よくある誤解\s*/g, '').trim();
+  const wrongMatch = text.match(/誤[：:]\s*(.+?)(?=正[：:]|$)/s);
+  const rightMatch = text.match(/正[：:]\s*(.+)/s);
+  if (wrongMatch && rightMatch) {
+    faqItems.push({
+      question: wrongMatch[1].trim(),
+      answer: rightMatch[1].trim(),
+    });
+  }
+}
+
+// 用語名を title から抽出（「〇〇とは？...」の「〇〇」部分）
+const termName = title.replace(/とは.*$/s, '').trim();
 ---
 
 <html lang="ja">
@@ -59,6 +91,7 @@ const hasRelated = relatedDeepPost || relatedGlossaryPosts.length > 0;
     <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
     <link href="https://fonts.googleapis.com/css2?family=Noto+Serif+JP:wght@700;800&display=swap" rel="stylesheet">
 
+    <!-- Article 構造化データ -->
     <script type="application/ld+json" set:html={JSON.stringify({
       "@context": "https://schema.org",
       "@type": "Article",
@@ -73,6 +106,37 @@ const hasRelated = relatedDeepPost || relatedGlossaryPosts.length > 0;
       "mainEntityOfPage": { "@type": "WebPage", "@id": Astro.url.href }
     })} />
 
+    <!-- DefinedTerm 構造化データ（用語定義） -->
+    <script type="application/ld+json" set:html={JSON.stringify({
+      "@context": "https://schema.org",
+      "@type": "DefinedTerm",
+      "name": termName,
+      "description": description,
+      "url": Astro.url.href,
+      "inDefinedTermSet": {
+        "@type": "DefinedTermSet",
+        "name": "株式投資用語集",
+        "url": "https://nikkei-note.com/blog/category/glossary/"
+      }
+    })} />
+
+    <!-- FAQPage 構造化データ（よくある誤解） -->
+    {faqItems.length > 0 && (
+      <script type="application/ld+json" set:html={JSON.stringify({
+        "@context": "https://schema.org",
+        "@type": "FAQPage",
+        "mainEntity": faqItems.map(item => ({
+          "@type": "Question",
+          "name": `${item.question}は正しい？`,
+          "acceptedAnswer": {
+            "@type": "Answer",
+            "text": item.answer
+          }
+        }))
+      })} />
+    )}
+
+    <!-- BreadcrumbList 構造化データ -->
     <script type="application/ld+json" set:html={JSON.stringify({
       "@context": "https://schema.org",
       "@type": "BreadcrumbList",
@@ -132,47 +196,57 @@ const hasRelated = relatedDeepPost || relatedGlossaryPosts.length > 0;
             <slot />
           </div>
 
+          <!-- 前後の用語解説ナビ -->
+          {(prevGlossary || nextGlossary) && (
+            <div class="g-post-nav">
+              <div class="g-post-nav-inner">
+                {prevGlossary ? (
+                  <a class="g-post-nav-item g-post-nav-prev" href={`/blog/${prevGlossary.id}/`}>
+                    <span class="g-post-nav-label">← 前の用語</span>
+                    <span class="g-post-nav-title">{prevGlossary.data.title.replace(/とは.*/, '')}</span>
+                  </a>
+                ) : <div />}
+                {nextGlossary ? (
+                  <a class="g-post-nav-item g-post-nav-next" href={`/blog/${nextGlossary.id}/`}>
+                    <span class="g-post-nav-label">次の用語 →</span>
+                    <span class="g-post-nav-title">{nextGlossary.data.title.replace(/とは.*/, '')}</span>
+                  </a>
+                ) : <div />}
+              </div>
+            </div>
+          )}
+
+          <!-- 今日の相場ノートCTA -->
+          {latestNikkei && (
+            <a class="g-nikkei-cta" href={`/blog/${latestNikkei.id}/`}>
+              <div class="g-nikkei-cta-icon">▧</div>
+              <div>
+                <div class="g-nikkei-cta-label">今日の相場ノートを読む</div>
+                <div class="g-nikkei-cta-title">{latestNikkei.data.title}</div>
+              </div>
+              <span class="g-nikkei-cta-arrow">→</span>
+            </a>
+          )}
+
         </article>
       </main>
 
       <!-- サイドバー -->
       <aside class="g-sidebar">
 
-        <!-- 相場ウィジェット（静的） -->
-        <div class="g-side-card">
-          <h3>今日の相場</h3>
-          <div class="g-market-list">
-            <div class="g-market-row">
-              <span class="g-market-label">日経平均<small>前日終値</small></span>
-              <div class="g-market-value">
-                38,487<br>
-                <span class="g-plus">+213 (+0.56%)</span>
-              </div>
-            </div>
-            <div class="g-market-row">
-              <span class="g-market-label">TOPIX<small>前日終値</small></span>
-              <div class="g-market-value">
-                2,736<br>
-                <span class="g-plus">+11 (+0.41%)</span>
-              </div>
-            </div>
-            <div class="g-market-row">
-              <span class="g-market-label">ドル円<small>15時30分</small></span>
-              <div class="g-market-value">
-                146.82<br>
-                <span class="g-minus">-0.34 (-0.23%)</span>
-              </div>
-            </div>
-            <div class="g-market-row">
-              <span class="g-market-label">NYダウ<small>前日終値</small></span>
-              <div class="g-market-value">
-                43,128<br>
-                <span class="g-plus">+84 (+0.20%)</span>
+        <!-- 最新の相場ノートへのリンク -->
+        {latestNikkei && (
+          <div class="g-side-card">
+            <h3>今日の相場ノート</h3>
+            <a class="g-related-item" href={`/blog/${latestNikkei.id}/`}>
+              <div class="g-related-meta">
+                {latestNikkei.data.pubDate.getFullYear()}年{latestNikkei.data.pubDate.getMonth() + 1}月{latestNikkei.data.pubDate.getDate()}日
               </div>
-            </div>
+              <div class="g-related-title">{latestNikkei.data.title}</div>
+            </a>
+            <a class="g-side-all-link" href="/blog/category/nikkei/">日経ノート一覧を見る →</a>
           </div>
-          <p class="g-market-note">※ 表示値は静的サンプルです。最新値は各証券会社サイトでご確認ください。</p>
-        </div>
+        )}
 
         <!-- 目次（JS生成） -->
         <div class="g-side-card" id="toc-card">
diff --git a/src/pages/blog/[...slug].astro b/src/pages/blog/[...slug].astro
index a38494c..6036635 100644
--- a/src/pages/blog/[...slug].astro
+++ b/src/pages/blog/[...slug].astro
@@ -21,7 +21,7 @@ const readTimeMin = Math.max(1, Math.ceil(plainText.length / 500));
 ---
 
 {post.data.category === 'glossary' ? (
-  <GlossaryPost {...post.data} readTimeMin={readTimeMin}>
+  <GlossaryPost {...post.data} readTimeMin={readTimeMin} body={post.body ?? ''}>
     <Content />
   </GlossaryPost>
 ) : (
diff --git a/src/pages/blog/category/glossary.astro b/src/pages/blog/category/glossary.astro
index e373895..450225f 100644
--- a/src/pages/blog/category/glossary.astro
+++ b/src/pages/blog/category/glossary.astro
@@ -79,9 +79,9 @@ function rowHasItems(chars: string): boolean {
 
 // まず読みたい3選（固定）
 const picks = [
-  { slug: '20260601-glossary-kiyodo', name: '寄与度',     desc: '日経平均の値動きを読む上で最も重要な概念。1銘柄が指数に与える影響を理解しよう。' },
-  { slug: '',                          name: 'TOPIXとは',  desc: '東証全体を反映する時価総額加重型指数。日経平均との比較で市場の歪みが見える。' },
-  { slug: '',                          name: 'リスクオフ', desc: '相場が不安定になるときに必ず登場する言葉。円高・金への資金逃避のメカニズムを解説。' },
+  { slug: '20260607-glossary-kiyodo',   name: '寄与度',     desc: '日経平均の値動きを読む上で最も重要な概念。1銘柄が指数に与える影響を理解しよう。' },
+  { slug: '20260607-glossary-topix',    name: 'TOPIXとは',  desc: '東証全体を反映する時価総額加重型指数。日経平均との比較で市場の歪みが見える。' },
+  { slug: '20260607-glossary-risk-off', name: 'リスクオフ', desc: '相場が不安定になるときに必ず登場する言葉。円高・金への資金逃避のメカニズムを解説。' },
 ];
 ---
 
diff --git a/src/pages/privacy.astro b/src/pages/privacy.astro
index f1f2bdd..c55a490 100644
--- a/src/pages/privacy.astro
+++ b/src/pages/privacy.astro
@@ -86,6 +86,8 @@
       border-left: 4px solid var(--navy);
     }
     p { margin: 0 0 16px; font-size: 15px; }
+    ul { margin: 0 0 16px; padding-left: 1.5em; }
+    ul li { font-size: 15px; margin-bottom: 6px; line-height: 1.8; }
     footer {
       background: var(--navy);
       color: #dce7f6;
@@ -111,22 +113,28 @@
   <div class="container">
     <div class="card">
       <h1>プライバシーポリシー</h1>
-      <div class="date">制定日：2026年5月</div>
+      <div class="date">制定日：2026年5月　最終更新：2026年6月</div>
 
-      <p>本日の日本株ノート（以下「当サイト」）は、ユーザーの個人情報の取り扱いについて、以下のとおりプライバシーポリシーを定めます。</p>
+      <p>今日の日本株ノート（以下「当サイト」）は、ユーザーの個人情報の取り扱いについて、以下のとおりプライバシーポリシーを定めます。</p>
 
-      <h2>個人情報の収集について</h2>
-      <p>当サイトでは、お問い合わせの際にお名前・メールアドレス等の個人情報をご入力いただく場合があります。収集した個人情報は、お問い合わせへの回答以外の目的には使用しません。</p>
-
-      <h2>広告について</h2>
-      <p>当サイトでは、第三者配信の広告サービス（Google AdSense）を利用しています。広告配信事業者はユーザーの興味に応じた広告を表示するため、Cookie（クッキー）を使用することがあります。Cookieを無効にする方法については、ご使用のブラウザのヘルプをご参照ください。</p>
-      <p>Google AdSenseに関するプライバシーについては、<a href="https://policies.google.com/privacy" target="_blank">Googleのプライバシーポリシー</a>をご確認ください。</p>
+      <h2>広告の配信について（Google AdSense）</h2>
+      <p>当サイトは、Google LLC が提供する第三者配信の広告サービス「Google AdSense」を利用しています。Google AdSense は、ユーザーの興味・関心に基づいた広告（インタレストベース広告）を表示するために、Cookie（クッキー）および広告識別子を使用することがあります。</p>
+      <p>Cookie により収集される情報には、当サイトおよび他のサイトへのアクセス情報が含まれますが、氏名・住所・電話番号・メールアドレスなど個人を直接特定できる情報は含まれません。</p>
+      <p>Google AdSense における Cookie の利用目的や、Cookie を無効にする方法については、以下をご参照ください。</p>
+      <ul>
+        <li><a href="https://policies.google.com/privacy" target="_blank" rel="noopener">Google プライバシーポリシー</a></li>
+        <li><a href="https://support.google.com/adsense/answer/7549925" target="_blank" rel="noopener">広告の Cookie に関する説明（Google）</a></li>
+        <li><a href="https://adssettings.google.com/authenticated" target="_blank" rel="noopener">Google 広告設定（オプトアウト）</a></li>
+      </ul>
 
       <h2>アクセス解析について</h2>
-      <p>当サイトでは、Cloudflare Analyticsを使用してアクセス状況を分析しています。このツールはCookieを使用せず、個人を特定する情報を収集しません。</p>
+      <p>当サイトでは、Cloudflare Analytics を使用してアクセス状況を分析しています。このツールは Cookie を使用せず、個人を特定する情報を収集しません。収集したデータはサイト改善のみに使用します。</p>
 
-      <h2>Cookieについて</h2>
-      <p>当サイトでは、広告配信のためにCookieを使用する場合があります。ユーザーはブラウザの設定によりCookieを無効にすることができますが、一部のサービスが正常に動作しない場合があります。</p>
+      <h2>Cookie（クッキー）について</h2>
+      <p>Cookie とは、ウェブサイトがブラウザに保存する小さなテキストデータです。当サイトでは、広告配信の最適化を目的として Cookie が使用される場合があります。ユーザーはブラウザの設定から Cookie を無効にすることができますが、その場合、一部の広告機能や表示が正常に動作しないことがあります。</p>
+
+      <h2>個人情報の収集について</h2>
+      <p>当サイトでは、お問い合わせの際にお名前・メールアドレス等の個人情報をご入力いただく場合があります。収集した個人情報は、お問い合わせへの回答以外の目的には使用しません。第三者への提供・開示は、法令に基づく場合を除き行いません。</p>
 
       <h2>免責事項</h2>
       <p>当サイトに掲載されている情報は、投資の勧誘を目的としたものではありません。掲載情報の正確性・完全性については万全を期しておりますが、その内容を保証するものではありません。当サイトの情報を利用した結果生じたいかなる損害についても、当サイトは責任を負いかねます。投資判断はご自身の責任においてお願いいたします。</p>
diff --git a/src/styles/glossary.css b/src/styles/glossary.css
index bdeb34b..5f0a16f 100644
--- a/src/styles/glossary.css
+++ b/src/styles/glossary.css
@@ -928,3 +928,95 @@
   cursor: default;
   font-weight: 400;
 }
+
+/* ─── 前後ナビ ─── */
+.g-post-nav {
+  border-top: 1px solid var(--border);
+  padding: 24px 36px;
+}
+.g-post-nav-inner {
+  display: grid;
+  grid-template-columns: 1fr 1fr;
+  gap: 12px;
+}
+.g-post-nav-item {
+  display: flex;
+  flex-direction: column;
+  gap: 4px;
+  padding: 14px 16px;
+  border: 1px solid var(--border);
+  border-radius: 12px;
+  background: var(--blue-2);
+  transition: box-shadow .15s;
+}
+.g-post-nav-item:hover { box-shadow: var(--shadow-sm); }
+.g-post-nav-next { text-align: right; }
+.g-post-nav-label {
+  font-size: 11px;
+  color: var(--muted);
+}
+.g-post-nav-title {
+  font-size: 13px;
+  font-weight: 700;
+  color: var(--navy);
+  line-height: 1.5;
+}
+
+/* ─── 相場ノートCTA ─── */
+.g-nikkei-cta {
+  display: flex;
+  align-items: center;
+  gap: 14px;
+  background: linear-gradient(135deg, #10233f 0%, #17375f 100%);
+  color: #fff;
+  margin: 0 36px 36px;
+  padding: 18px 22px;
+  border-radius: 14px;
+  text-decoration: none;
+  transition: opacity .15s;
+}
+.g-nikkei-cta:hover { opacity: .9; }
+.g-nikkei-cta-icon {
+  width: 40px;
+  height: 40px;
+  border-radius: 10px;
+  background: rgba(255,255,255,.15);
+  display: grid;
+  place-items: center;
+  font-size: 18px;
+  flex-shrink: 0;
+}
+.g-nikkei-cta-label {
+  font-size: 11px;
+  color: #9ec4e8;
+  margin-bottom: 3px;
+}
+.g-nikkei-cta-title {
+  font-size: 14px;
+  font-weight: 700;
+  line-height: 1.5;
+}
+.g-nikkei-cta-arrow {
+  margin-left: auto;
+  font-size: 18px;
+  color: #9ec4e8;
+  flex-shrink: 0;
+}
+
+/* ─── サイドバー 全一覧リンク ─── */
+.g-side-all-link {
+  display: block;
+  margin-top: 10px;
+  font-size: 12px;
+  color: var(--navy);
+  font-weight: 700;
+  text-decoration: underline;
+  text-underline-offset: 3px;
+}
+
+/* ─── レスポンシブ対応 ─── */
+@media (max-width: 640px) {
+  .g-post-nav { padding: 16px; }
+  .g-post-nav-inner { grid-template-columns: 1fr; }
+  .g-nikkei-cta { margin: 0 0 24px; border-radius: 10px; }
+}
